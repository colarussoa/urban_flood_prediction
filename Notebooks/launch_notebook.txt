Python version is 3.12, so if you don't have it installed, run:
py -3.12 -V

Go to folder:
#local
cd C:\Users\sabio\Documents\urban_flooding_prediction\Data\weather_sensors\API_pulls
#lab
S:
cd S:\Alec\urban_flooding_prediction\analysis

Make environment in folder, Note: if you have a bunch of python installations and didn't set 3.12 as your default like me, run the below:
python -m venv venv
the below:
py -3.12 -m venv venv

activate:
venv\Scripts\activate.bat

how to get libraries in a venv from a requirements.txt:
pip install -r requirements.txt

register kernel:
python -m ipykernel install --user --name=my_venv --display-name="Python (venv)"

Launch notebooks:
jupyter notebook

Notes from first installation:

install notebook:
pip install notebook

install kernel:
pip install ipykernel

Get requirements.txt
pip freeze > requirements.txt




